const Font = {
  mitrExtraLight: (size = 1, useCompareWidth) => ({
    fontFamily: 'Mitr-ExtraLight',
    fontSize: size,
  }),
  mitrLight: (size = 1, useCompareWidth) => ({
    fontFamily: 'Mitr-Light',
    fontSize: size,
  }),
  mitrRegular: (size = 1, useCompareWidth) => ({
    fontFamily: 'Mitr-Regular',
    fontSize: size,
  }),
  mitrMedium: (size = 1, useCompareWidth) => ({
    fontFamily: 'Mitr-Medium',
    fontSize: size,
  }),
  mitrSemiBold: (size = 1, useCompareWidth) => ({
    fontFamily: 'Mitr-SemiBold',
    fontSize: size,
  }),
  mitrBold: (size = 1, useCompareWidth) => ({
    fontFamily: 'Mitr-Bold',
    fontSize: size,
  }),
};

export default Font;
